#include<stdio.h>

int main() {
    // Define file paths
    const char* file1 = "nav_data.csv";
    const char* file2 = "obs_data.csv";

    // Call the trail1 function
    trail1(file1, file2);

    return 0;
}